[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = [
    # pydantic-ai renamed OpenAIModel -> OpenAIChatModel; silence until we migrate.
    "ignore::DeprecationWarning:agents",
    "ignore::DeprecationWarning:app.agents",
]
